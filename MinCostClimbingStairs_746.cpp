#include <vector>

class Solution {
public:
    int minCostClimbingStairs(vector<int>& cost) {
        int length = cost.size();
        if(length <= 4){
            for()
        }
        int result = 0;
        return result;
    }
};